const CCRCore = artifacts.require('./CCRCore.sol');

contract('CCR Core', () =>{
    let CCRCoreInstance

    before('reinitialize each time', async () => {
        let CCRCoreInstance
    })

    it('', async function () {
    })
});